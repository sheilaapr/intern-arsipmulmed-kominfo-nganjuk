document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-confirm]').forEach(el=>el.addEventListener('click',e=>{if(!confirm(el.dataset.confirm||'Apakah Anda yakin?'))e.preventDefault()}));document.querySelectorAll('[data-autohide]').forEach(el=>setTimeout(()=>{el.style.opacity='0';setTimeout(()=>el.remove(),250)},3500));document.querySelectorAll('form').forEach(form=>form.addEventListener('submit',()=>{const btn=form.querySelector('button[type="submit"]');if(btn&&!btn.dataset.loading){btn.dataset.loading='1';btn.dataset.old=btn.innerHTML;btn.innerHTML='<i class="fas fa-spinner fa-spin"></i> Memproses...';btn.disabled=true}}));});
